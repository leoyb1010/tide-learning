# 潮汐学习商业化前全链路审计（2026-08-21）

## 结论

- **产品/用户接入准备度：92 / 100**（可进入受控真实用户 Beta）
- **正式付费上线：CONDITIONAL NO-GO**，在 Stripe 密钥、Webhook 沙箱验收和历史费用对账完成前不得开放真实付款。
- 原始大更新后的估算基线为 **74 / 100**：造课调用爆炸、失败整课推翻、支付状态误导、笔记/上传边界和测试稳定性不足。

## 评分证据

| 维度 | 权重 | 得分 | 权威证据 |
| --- | ---: | ---: | --- |
| 核心价值（造课/上传/学习/考试/笔记） | 25 | 24 | 标准造课瘦身；真实 10 节失败课恢复至 ready；上传/进度/测验/笔记 E2E 全通过 |
| 可靠性与数据一致性 | 20 | 19 | 93 个测试文件、746 项通过；SQLite quick_check=ok；迁移最新；课程/Job/积分采用事务和 fencing |
| 安全与隐私 | 20 | 18 | IDOR/CSRF/SSRF/附件魔数/Base64/路径穿越/私有内容门控；npm audit 0；仍为单机 SQLite 部署 |
| 易用性与化繁为简 | 15 | 14 | 标准造课移除导演/双评审/LLM HTML，失败自动可靠回落；支付不可用时禁用 CTA 并说明体验环境 |
| 商业化与运营 | 10 | 8 | 真实定价、优惠券和订单状态机；管理员运营健康指标；历史失败课程已恢复为 0；Stripe 未配置，不能真实收款 |
| 可维护性与可观测性 | 10 | 9 | CI、全量测试串行稳定、版本/健康接口、AI 诊断和账务/失败任务可见；PR 尚待合并 master |
| **总计** | **100** | **92** |  |

## 本轮完成的简化与修复

### 课程制作

- 标准课改为：1 次大纲 + 每节 1 次作者，协议损坏最多补 1 次。
- 标准课移除实时导演 Agent、双评审 Agent、整课 LLM 终审和 LLM HTML。
- `gpt-5.6-sol` 使用 low reasoning；429 按 Retry-After 退避。
- JSON 损坏、429、安全/来源误判时生成完整可靠课节，不落占位文案。
- 失败章节独立保存，不再让最后一步推翻整门课程。

### 上传与导入

- 管理员章节只接受 video/article；生产视频必须是真 MP4/WebM 且资产校验通过。
- 时长、正文、试看、文件类型运行时校验。
- 附件严格 Base64、魔数、文件名净化、路径隔离和下载限流。
- PDF/URL 导入最终写入时事务内二次校验免费配额，关闭并发越额。

### 学习与考试

- 进度按视频/翻页隔离，付费和私有课节写入门控。
- 真实浏览器验证课程详情、免费试学、翻页、进度、笔记入口无控制台错误。
- 考试 GET 不泄漏答案；跨用户试卷 404；提交、成绩单、错题写入通过。
- 错题本非法游标返回 400，不伪装成空数据。

### 笔记与复习

- 正文自动保存同步更新 excerpt；标签和正文在同一事务。
- 标题、正文、剪藏、截帧、时间戳、标签数量增加边界。
- 笔记、附件、导入、导出、笔记本、标签和跨用户隔离均完成真实请求验证。
- SSRF 内网链接导入稳定拒绝。

### 商业与运营

- 服务端不再信任客户端选择支付渠道。
- 未配置 Stripe 双密钥时不创建订单，定价页禁用付款并明确“产品体验环境”。
- 权益表与真实免费造课额度对齐。
- 管理员看板/诊断增加：待对账费用、过期预占、负余额、运行中任务、失败课程。

### 历史失败课程清理

- 新增默认 dry-run 的 `recover-failed-standard-courses.mts` 运维工具。
- 两门历史失败课程已恢复到 `ready`，全部课节内容与确定性 HTML 可用。
- 当前 `Course.genStatus=failed` 数量为 0。

## 当前必须处理的正式收款门槛

1. 配置 `STRIPE_SECRET_KEY`、`STRIPE_WEBHOOK_SECRET`，将 `NEXT_PUBLIC_PAY_CHANNEL` 改为 `stripe`。
2. 在 Stripe 测试环境完成成功支付、取消、重复 webhook、退款和金额/币种错配验收。
3. 人工核对并处理当前 7 条 `LlmBillingReconciliation.status=pending` 历史供应商超时记录。
4. 合并 PR #18 到受保护的 `master`，确保线上代码与默认分支一致。
5. 正式商业部署移除 `ALLOW_LOCAL_PRODUCTION=1`，使用正式持久化目录、监控和备份策略。

## 上线分级

- **真实用户 Beta：GO**（支付禁用，允许注册、免费试学、免费造课体验、学习、考试、笔记）。
- **正式收费：NO-GO，等待上述 5 项门槛完成。**

## 2026-09-05 多角色审计补充：商业化与运营观测

### 本轮结论

- **受控 Beta：GO**。注册、免费试学、学习进度、考试、笔记、兑换码和积分账务链路已有服务端门控。
- **真实收费：仍 CONDITIONAL NO-GO**。支付代码已具备 Stripe provider、原始 body 验签和 webhook 幂等，但部署凭据、沙箱回放和历史供应商账务对账仍是发布闸门。
- **运营观测：已补齐 API 收入聚合**。`GET /api/admin/dashboard` 现在额外返回 `paidOrders`、`grossRevenueCents`、`discountsCents`，用于收入、付费订单和优惠影响的服务端观测。

### P0 发布前清单

1. Stripe 测试环境完成成功支付、取消、重复 webhook、退款、金额/币种错配回放，并核对订单、订阅、权益和审计日志。
2. 处理全部 `LlmBillingReconciliation.status=pending`，确认无负余额、过期预占和孤儿运行任务。
3. 生产使用绝对持久化路径、`STORAGE_MODE=local`、加密备份异地复制、外部告警和值班责任人。
4. 发布构建、数据库迁移和默认分支提交保持一致；生产环境不得启用 mock 支付。

### P1 运营改进

- 后台页面可将 API 新增收入字段接入卡片或按日/周聚合图，避免只看订阅人数而无法判断现金收入。
- 增加支付渠道、币种、退款原因和优惠券维度的导出，供财务月结与客服核对。
- 对订单 webhook 延迟、失败、重复和退款事件设置外部告警阈值。
